// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#179bd7", // Blue
    },
    secondary: {
      main: "#253b80", // Pink
    },
    text: {
      primary: "#000", // Black
    },
  },
});

export default theme;
