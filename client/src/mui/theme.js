// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#179bd7",
    },
    secondary: {
      main: "#253b80",
    },
    text: {
      primary: "#000",
    },
  },
});

export default theme;
